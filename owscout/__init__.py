"""owscout — Overwatch 2 composition extraction from in-client replays.

Sibling tool to ``faceit-sync``. See ``SPEC.md`` at the repo root for the
authoritative design. This package is being built in the order given by
SPEC §13; only step 1 (``owscout calibrate``) is implemented so far.
"""

from __future__ import annotations

__version__ = "0.1.0"
